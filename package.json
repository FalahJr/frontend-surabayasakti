{
  "name": "sinar-surabayasakti-website",
  "version": "1.0.0",
  "description": "Company profile website PT Sinar Surabayasakti - static HTML + Tailwind CSS (build process) + vanilla JS.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
